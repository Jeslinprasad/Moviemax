apiVersion: v1
kind: Service
metadata:
  name: moviemax-app
  labels:
    app: moviemax-app
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30007
  selector:
    app: moviemax-app
